server:
  port: 8081

swagger.show: true

spring:
  profiles:
    active: localhost
---
spring:
  profiles: localhost
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/websit?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: LIU_123_abc
  redis:
    password:

